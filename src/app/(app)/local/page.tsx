// Aba Local (pos-venda): produtos em assistencia fisica na empresa (Aracatuba).
import { Local } from "@/components/local/Local";

export const dynamic = "force-dynamic";

export default function LocalPage() {
  return <Local />;
}
